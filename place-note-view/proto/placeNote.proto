syntax = "proto3";

package placeNote;

service UserService {
    rpc AuthGoogleCode(AuthGoogleCodeRequest) returns (AuthGoogleCodeResponse) {}
}

message AuthGoogleCodeRequest {
    string authCode = 1;
}

message AuthGoogleCodeResponse {
    string authGoogleToken = 1;
}
